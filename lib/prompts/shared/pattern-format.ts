export const PATTERN_FORMAT_DOC = `
- Pattern format: "start...end" → matches text that starts with "start" and ends with "end"
- Matching is case-insensitive and ignores extra whitespace/newlines.
`
