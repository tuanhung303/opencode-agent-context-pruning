export default `
Restores previously pruned tool outputs to the conversation context.

## Parameters
- hashes: Array of hash strings (e.g., ["r_a1b2c", "g_d4e5f"])

## Examples
restore_tool(["r_a1b2c", "g_d4e5f"])
`
